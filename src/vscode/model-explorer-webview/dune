; generated by drom from package skeleton 'js_program'

(executable
  (name model_explorer_webview)
  (libraries model-explorer-js-stubs )
  (modes js)
  (preprocess (pps gen_js_api.ppx))
  )

(install
 (files (model_explorer_webview.bc.js as www/js/model-explorer-webview.js))
 (section share)
 (package model-explorer-webview))


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} -I +unix unix.cma %{script}))))

(documentation
  (package model-explorer-webview))



